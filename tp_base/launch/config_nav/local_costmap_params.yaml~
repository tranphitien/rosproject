local_costmap:
  global_frame: /world_frame
  robot_base_frame: /tp_robot
  update_frequency: 4.0
  publish_frequency: 2.0
  static_map: true
  rolling_window: true
  width: 4.0
  height: 4.0
  resolution: 0.05

#the publish_frequency parameter determines the frequency to publish information.
#the rolling_window parameter is used to keep the costmap centered on the robot when it is moving around the world
#And you can configure the dimensions and the resolution of the costmap with the width, heigh and resolution parameter (meter)
